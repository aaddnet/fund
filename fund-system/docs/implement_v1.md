# Implementation Notes (V1)

- Use PostgreSQL as the only database.
- Keep all timestamps in UTC.
- Treat NAV snapshots as immutable records.
- Build backend with FastAPI + SQLAlchemy.
- Build frontend with Next.js pages router.
